#include <iostream>

using namespace std;
int main (){
    int t;
    cin >> t;
    // input a[] and b[]
    
    /**
     * filter1: eliminate easy cases ie a|b <1, a&b=3n, etc 
     * ??strat 1: calc a:b, figure out remainders (a/b)/(1/2)
     * filter2: if (a-b) > min(a,b) bc min not enough to compensate diff even with -1s 
     * key values: (a - b), min(a,b) % 3,  
    */
    
    return 0;
}